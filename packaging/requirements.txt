# Runtime dependencies for the DigiRaksha launcher venv.
# Installed by launcher.py into <runtime root>\venv on first run.
# NOTE: torch is installed separately by the launcher from the CPU-only index
# (https://download.pytorch.org/whl/cpu) to avoid the multi-GB CUDA wheel.

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
email-validator>=2.0

# ML / audio pipeline
numpy>=1.26,<3
scipy>=1.11
librosa>=0.10
soundfile>=0.12
opencv-python>=4.9,<5
faster-whisper>=1.0
imageio-ffmpeg>=0.4.9
scikit-learn>=1.4
joblib>=1.3
pyttsx3>=2.90

# Reports
reportlab>=4.0
